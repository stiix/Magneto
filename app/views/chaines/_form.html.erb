<%= form_for(@chaine) do |f| %>
  <% if @chaine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@chaine.errors.count, "error") %> prohibited this chaine from being saved:</h2>

      <ul>
      <% @chaine.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table>
    <tr class = "entete">
      <th> <%= f.label :nom %></th>
      <th> <%= f.label :numero %></th>
      <th></th>
    </tr>
    <tr class = "table1">
      <td> <%= f.text_field :nom %> </td>
      <td> <%= f.number_field :numero %> </td>
      <td> <%= f.submit :class=>"btn btn-primary btn-chaine" %> </td>
    </tr>
  </table>
<% end %>
<%= link_to 'Interface administrateur', { :controller=>:admin }, :class=>"btn btn-primary btn-chaine" %>  
