<%= form_for(@personne) do |f| %>
  <% if @personne.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@personne.errors.count, "error") %> prohibited this personne from being saved:</h2>

      <ul>
      <% @personne.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :prenom %><br />
    <%= f.text_field :prenom %>
  </div>
  <div class="field">
    <%= f.label :surnom %><br />
    <%= f.text_field :surnom %>
  </div>
  <div class="field">
    <%= f.collection_select :type_personne_id, TypePersonne.order("libelle").all, :id, :libelle %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
